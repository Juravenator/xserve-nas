apiVersion: v1
kind: Service
metadata:
  name: plex-media-server
spec:
  type: ClusterIP
  ports:
  - name: pms
    port: 32400
    targetPort: 32400
    protocol: TCP
  selector:
    app.kubernetes.io/name: plex-media-server
    app.kubernetes.io/instance: plex
